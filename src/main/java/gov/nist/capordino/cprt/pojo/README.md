# src - POJO

This directory contains code for the POJO (Plain Old Java Object) classes that serve as intermediary objects when reading the CPRT JSON file, storing its information, and converting it into OSCAL format.